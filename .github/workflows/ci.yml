name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  quality:
    runs-on: ubuntu-latest
    env:
      # CI DATABASE: dummy Postgres to satisfy env validation
      DATABASE_URL: "postgresql://user:pass@localhost:5432/db?schema=public"
      DIRECT_URL: "postgresql://user:pass@localhost:5432/db?schema=public"

      NEXTAUTH_URL: "http://localhost:3000"
      NEXTAUTH_SECRET: "ci-nextauth-secret-min-32-chars-xxxxxxxxxxxxxxxx"

      # Required in production by your env validation, set a safe CI fallback
      INTERNAL_CRON_SECRET: "ci-internal-cron-secret-xxxxxxxxxxxxxxxx"

      # Stripe (safe dummy values for CI build-time validation)
      STRIPE_SECRET_KEY: "sk_test_dummy"
      STRIPE_WEBHOOK_SECRET: "whsec_dummy"
      STRIPE_PRICE_ID_PRO: "price_dummy"
      STRIPE_PRICE_ID_PRO_YEAR: "price_dummy_yearly"
      STRIPE_PRODUCT_ID_PRO: "prod_dummy"

      # Optional / used by app
      APP_URL: "http://localhost:3000"
      OPENAI_API_KEY: "sk-dummy"

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npm run db:generate

      - name: Run CI gates (lint, typecheck, test, build)
        run: npm run ci
